# REST + Socket.IO base URL. Falls back to localhost for dev.
VITE_API_URL=http://localhost:4000

# Optional: explicit origin string (used only in dev docs/error messages).
VITE_APP_ORIGIN=http://localhost:5173
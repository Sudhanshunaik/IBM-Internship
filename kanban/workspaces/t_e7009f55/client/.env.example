# Client environment variables

# API base — proxied in dev, full URL in production.
VITE_API_URL=http://localhost:4000

# Socket.IO path (must match server's SOCKET_PATH).
VITE_SOCKET_PATH=/socket.io

# Default scene id to load on boot (optional).
VITE_DEFAULT_SCENE_ID=